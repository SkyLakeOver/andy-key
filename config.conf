# Конфигурационный файл andy-key
# Формат: KEY=VALUE

# Путь к файлу базы данных SQLite
DB_PATH=data.db

# Порт веб-интерфейса (необязательно, по умолчанию 9000)
# PORT=9000

# Путь к директории с логами (необязательно, по умолчанию "logs")
# LOGS_DIR=logs
